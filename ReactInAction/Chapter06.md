# Chapter 6 - Integrating third-party libraries with React

## Mike

This chapter was lackluster at best. It was mostly filled with setting up a convoluted example; in my opinion it would have been more beneficial for the chapter to go over the `ref` property more in depth and the possible use-cases, instead focusing heavily on one example, especially when that example has a lot of code surrounding it and clouding the helpful information.

It's also worth noting that functional components can utilize `refs` with the `useRef` hook. The hook can also be used to create instance variables in functional components as well.
